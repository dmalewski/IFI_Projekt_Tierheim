<html>
    <head>
    <meta charset="utf-8">

    <link rel="icon" type="image/vnd.microsoft.icon" href="/images/paw_new.png">

    <link href="https://fonts.googleapis.com/css?family=Dosis|Josefin+Sans" rel="stylesheet"> 

    <title>Ausgabe</title>

</head>
    <body>
        <h1>Diese Hunde wurden gefunden:</h1>

        <% if(message) { %>
            <%= message %>
        <% } %>

        <% dogs.forEach(function (dog) { %>
            <li>
                <%= dog.name %>, <%= dog.gender %>, <%= dog.bread %>, <%= dog.age %>, <%= dog.height %>
                <img src="<%= dog.img %>" style="height: 100px; width: 100px;"/>
            </li>                        
        <% }) %>
    </body>
</html>